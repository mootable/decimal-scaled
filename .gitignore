/target
Cargo.lock
/bench-history/target
/bench-history/Cargo.lock
/bench-history-results

# Profiler artefacts (local-only)
*.stackdump
flamegraph*.svg
perf.data*
profile.json*
*.dtrace

# scratch / local-only generated data — benches, sweeps, traces, profiling output
/tmp/
/bench-compare/target

# Private working notes. These share this working tree but are tracked by a
# separate private repository, never by this one.
/research/
/decisions/
/CLAUDE.md

# Python bytecode cache (scripts/)
__pycache__/
