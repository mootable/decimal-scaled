//! (Emptied during the `BigInt` unification.)
//!
//! The `WideInt` / `WideStorage` impls for `Int<N>` that lived here moved
//! into [`crate::int::types::traits`], alongside the unified `BigInt`
//! trait. This module is now a stub and will be removed once the merge
//! settles.
