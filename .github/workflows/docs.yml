name: docs

# Builds the API documentation (rustdoc) plus the prose guides in
# `docs/` and publishes them to GitHub Pages.
#
# Triggers:
#   * any pushed `v*` release tag (the deploy script in
#     `scripts/deploy.ps1` pushes such a tag as its final git step);
#   * a push to `main` (keeps the published docs current between
#     releases);
#   * manual dispatch from the Actions tab.
#
# One-time repo setup: Settings -> Pages -> Build and deployment ->
# Source: "GitHub Actions".

on:
  push:
    tags:
      - 'v*'
    branches:
      - main
  workflow_dispatch:

# Allow only one concurrent deployment.
concurrency:
  group: pages
  cancel-in-progress: true

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Install Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install MkDocs Material
        run: pip install mkdocs-material pymdown-extensions

      - name: Build rustdoc
        # `--all-features` is avoided: `experimental-floats` needs
        # the nightly toolchain. `wide` + `x-wide` + `xx-wide` +
        # `macros` is the widest stable-buildable set and documents
        # every shipped tier (D9 through D1231).
        run: cargo doc --no-deps --features wide,x-wide,xx-wide,macros --workspace
        env:
          # Fail the build on any rustdoc warning so broken intra-doc
          # links never reach the published site.
          RUSTDOCFLAGS: "-D warnings"

      - name: Stage root-level narrative files into docs/
        # MkDocs only reads under `docs/`. Copy ROADMAP, CHANGELOG,
        # and ALGORITHMS in so they become first-class pages
        # (referenced from nav in mkdocs.yml). README is NOT
        # staged — docs/README.md already exists as the guides
        # index; the root README is reached via the GitHub link
        # in the header.
        run: |
          set -euo pipefail
          [ -f ROADMAP.md ]    && cp ROADMAP.md    docs/ROADMAP.md
          [ -f CHANGELOG.md ]  && cp CHANGELOG.md  docs/CHANGELOG.md
          [ -f ALGORITHMS.md ] && cp ALGORITHMS.md docs/ALGORITHMS.md

      - name: Build MkDocs site
        run: mkdocs build --strict --site-dir _site

      - name: Layer the rustdoc tree under /api/
        run: |
          set -euo pipefail
          cp -r target/doc _site/api

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
