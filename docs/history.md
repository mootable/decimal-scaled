# History

How correctness and performance have moved release over release.

Generated from CI: the *history-gates* run measures the live crate beside pinned
past releases over the same golden set, and commits the per-version results to
`results/history/`, which this page renders. Refreshed on each release PR.

<!-- The cross-version table (per function: pass / fail / median ns, per
     version) lands here once the history pipeline is wired (see
     research/bench_docs_pipeline.md, step 4). Until the first history-gates run
     populates results/history/, this page is intentionally a stub. -->

For the full list of changes, see the [Changelog](CHANGELOG.md).
